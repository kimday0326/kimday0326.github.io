# 블로그 글쓰기 가이드

## 1. 파일 위치

모든 포스트는 **`_posts/`** 디렉토리 안에 저장합니다.

```
kimday0326.github.io/
└── _posts/
    ├── 2026-05-04-my-first-post.md   ← 여기에 저장
    └── 2026-05-04-another-post.md
```

---

## 2. 파일 이름 규칙

파일 이름은 반드시 아래 형식을 따라야 합니다:

```
YYYY-MM-DD-제목.md
```

**예시:**
```
2026-05-04-hello-world.md
2026-06-15-spring-review.md
2026-12-31-year-end-recap.md
```

> 날짜가 파일명에 포함되어야 Jekyll이 포스트로 인식합니다.  
> 제목 부분은 영문 소문자와 하이픈(-)을 사용하는 것을 권장합니다.

---

## 3. 포스트 템플릿 (복붙해서 사용)

아래 내용을 파일 상단에 붙여넣고 내용을 수정하세요.

```markdown
---
layout: post
title: "여기에 포스트 제목을 입력하세요"
date: 2026-05-04 12:00:00 +0900
category: 카테고리명
tags: [태그1, 태그2, 태그3]
excerpt: "카드 목록에 표시될 요약 문장을 여기에 입력합니다."
thumbnail: "https://이미지-url.com/image.jpg"
---

여기서부터 본문을 작성합니다.

## 첫 번째 섹션

내용을 작성하세요.
```

---

## 4. Frontmatter 필드 설명

파일 최상단의 `---` 사이에 작성하는 메타데이터입니다.

| 필드 | 필수 여부 | 설명 |
|------|----------|------|
| `layout` | **필수** | 항상 `post`로 고정 |
| `title` | **필수** | 포스트 제목 (큰따옴표로 감싸기) |
| `date` | **필수** | 작성일시 (`YYYY-MM-DD HH:MM:SS +0900`) |
| `category` | 선택 | 카테고리 하나 (카드에 표시됨) |
| `tags` | 선택 | 태그 목록 (`[태그1, 태그2]` 형식) |
| `excerpt` | 선택 | 카드 요약문 (없으면 본문 앞부분 자동 추출) |
| `thumbnail` | 선택 | 카드에 표시될 대표 이미지 URL (없으면 제목 첫 글자 표시) |

---

## 5. 지원되는 마크다운 기능

### 목차 (TOC)

`h2` (`##`), `h3` (`###`) 헤딩을 사용하면 우측 사이드바에 **목차가 자동 생성**됩니다.

```markdown
## 첫 번째 섹션
### 세부 내용

## 두 번째 섹션
```

### 이미지

```markdown
![이미지 설명](https://이미지-url.com/image.jpg)
```

### 코드 블록

````markdown
```javascript
const hello = "world";
```
````

### 인용문

```markdown
> 인용할 내용을 여기에 작성합니다.
```

### 표

```markdown
| 열1 | 열2 | 열3 |
|-----|-----|-----|
| 값1 | 값2 | 값3 |
```

---

## 6. 포스트 작성 예시

**파일명:** `_posts/2026-06-01-my-dev-log.md`

```markdown
---
layout: post
title: "나의 첫 개발 일지"
date: 2026-06-01 10:00:00 +0900
category: dev
tags: [개발, 일지, jekyll]
excerpt: "블로그를 시작하면서 느낀 점들을 정리했습니다."
thumbnail: "https://picsum.photos/seed/devlog/800/450"
---

## 시작하며

오늘부터 개발 일지를 써보기로 했습니다.

## 배운 것

- Jekyll은 정적 사이트 생성기입니다.
- `_posts/` 폴더에 마크다운 파일을 넣으면 자동으로 포스트가 됩니다.

## 마치며

꾸준히 기록하는 것이 목표입니다.
```

---

## 7. 포스트 정렬

포스트는 **날짜 기준 최신순**으로 자동 정렬됩니다. 파일명의 날짜가 기준이 됩니다.
